<!DOCTYPE html>

<html>
{% spaceless %}
{% set currentRoute = app.request.attributes.get('_route') %}
{% set mainMenuRoute = mainMenuRoute|default(currentRoute) %}
{% endspaceless %}
<head>
	<meta charset="UTF-8">
	<title>{{ pageTitle }} – {{ globals.siteName }}</title>
	<link rel="stylesheet" href="{{ asset('admin/themes/default/theme.css') }}">
    <script src="{{ asset('bundles/jquery/jquery.min.js') }}"></script>
    <script src="{{ asset('bundles/jquery/jquery-ui.min.js') }}"></script>
    <script src="{{ asset('bundles/jquery/jquery.cookie.js') }}"></script>
</head>
<body class="page">

    <div class="page__layout">

        <header class="page__header">
            <div class="cms-logo">
                <a href="{{ path('admin.dashboard') }}" class="cms-logo__link">
                    <img src="{{ asset('admin/themes/default/img/logo.png') }}"
                        alt="Eresus" class="cms-logo__image">
                    EresusCMS ${product.version}
                </a>
            </div>

            <h1 class="site-title">{{ globals.siteName }}</h1>

            <nav class="main-menu">
                <ul class="main-menu__items">
                    <li class="main-menu__item{% if 'admin.dashboard' == mainMenuRoute %} main-menu__item_is_current{% endif %}">
                        <a href="{{ path('admin.dashboard') }}" class="main-menu__link">
                            <span class="icon icon_img_home"></span>
                            Обзор
                        </a>
                    </li>

                    <li class="main-menu__item{% if 'admin.files' == mainMenuRoute %} main-menu__item_is_current{% endif %}">
                        <a href="{{ path('admin.files') }}" class="main-menu__link">
                            <span class="icon icon_img_documents"></span>
                            Файлы
                        </a>
                    </li>

                    <li class="main-menu__item{% if 'admin.settings' == mainMenuRoute %} main-menu__item_is_current{% endif %}">
                        <a href="{{ path('admin.settings') }}" class="main-menu__link">
                            <span class="icon icon_img_settings"></span>
                            Настройки
                        </a>
                    </li>
                </ul>
            </nav>

            <div class="user-box">
                <span class="user-box__intro">Вы</span>
                <span class="user-box__username">{{ app.user.username }}</span>
                <div class="user-box__links">
                    <a href="{{ path('admin.logout') }}" class="user-box__exit">Выйти</a>
                </div>
            </div>

        </header>

        <nav class="page__sidebar">

            {% block sidebar %}
            <div class="box">
                <div class="box__header">Разделы сайта</div>
                <div class="box__content sections">
                    <ul class="sections__children">
                        {#% include 'CmsBundle:Menus:Sections.html.twig' with {'section': rootSection, 'level': 0} only %#}
                    </ul>
                </div>
            </div>
            {% endblock %}
        </nav>

        <div class="page__content">
            <noscript>
                <div class="message message_type_error">
                    В вашем браузере не работает JavaScript. Без JavaScript работа с панелью управления
                    сайтом невозможна, и может привести к потере информации!
                </div>
            </noscript>

            {% block contents %}
            {% endblock %}

        </div>
    </div>

	<script src="{{ asset('admin/themes/default/theme.js') }}"></script>
	<script src="{{ asset('bundles/eresus/admin.js') }}"></script>
	{#{ body }#}
</body>
</html>